
Trong JavaScript, cấu trúc dữ liệu nào phù hợp nhất để biểu diễn một danh sách các công việc cần làm, trong đó bạn muốn dễ dàng thêm và xóa các công việc từ đầu hoặc cuối danh sách?
Cho đoạn code JavaScript sau: `const map = new Map(); map.set('a', 1); map.set('b', 2); map.set('a', 3); console.log(map.get('a'));` . Kết quả in ra console là gì?
Cấu trúc dữ liệu nào hoạt động theo nguyên tắc LIFO (Last In, First Out)?
Cho một mảng đã được sắp xếp `[1, 3, 5, 7, 9]`. Thuật toán nào hiệu quả nhất để tìm kiếm số 7 trong mảng này?
Trong JavaScript, cách triển khai nào tốt nhất để tạo một hàng đợi (Queue)?
Độ phức tạp thời gian tốt nhất để chèn một phần tử vào đầu một linked list là gì?
Khi nào nên sử dụng Set thay vì Array trong JavaScript?
Cho đoạn code JavaScript sau: `const arr = [5, 2, 8, 1, 9]; arr.sort(); console.log(arr);`. Kết quả in ra console là gì?
Cấu trúc dữ liệu nào sau đây phù hợp nhất để biểu diễn mối quan hệ cha-con trong một hệ thống phân cấp?
Độ phức tạp thời gian trung bình để tìm kiếm một phần tử trong một hash table là gì?
What method is used to add elements to the end of an array in JavaScript?
Which array method removes the last element from an array?
What method is used to add elements to the end of an array in JavaScript?
Which array method removes the last element from an array?
Trong JavaScript, khi nào bạn nên sử dụng một `WeakMap` thay vì một `Map` thông thường?
Cho đoạn code JavaScript sau: `const queue = []; queue.push(1); queue.push(2); queue.shift(); console.log(queue.length);` Kết quả in ra console là gì?
Trong JavaScript, cấu trúc dữ liệu nào phù hợp nhất để kiểm tra xem một phần tử có tồn tại trong một tập hợp lớn các phần tử duy nhất hay không, với hiệu suất cao?
Thuật toán sắp xếp nào sau đây có hiệu suất tốt nhất trong trường hợp dữ liệu đã gần như được sắp xếp?
Trong JavaScript, khi nào nên sử dụng một mảng (Array) thay vì một linked list?
Cho đoạn code JavaScript sau: `const obj = { a: 1, b: 2 }; delete obj.a; console.log('a' in obj);` Kết quả in ra console là gì?
Cấu trúc dữ liệu nào sau đây phù hợp nhất để triển khai một bộ nhớ cache sử dụng thuật toán Least Recently Used (LRU)?
Trong JavaScript, độ phức tạp thời gian trung bình để tìm kiếm một phần tử trong một binary search tree cân bằng là gì?
Cho đoạn code JavaScript sau: `const arr = [1, 2, 3, 4, 5]; arr.splice(2, 1); console.log(arr);` Kết quả in ra console là gì?
Trong JavaScript, nếu bạn cần triển khai một đồ thị (graph), cấu trúc dữ liệu nào sau đây thường được sử dụng để biểu diễn các cạnh?